# 装箱推荐规则记录

本文档记录页头打印模块的装箱推荐口径，后续新增仓库实际样本时继续追加校准。

## 当前规则顺序

1. 页面“预设规则”优先级最高，命中后直接使用预设箱型和气泡袋。
2. 仓库实际样本校准层第二优先级，命中已知组合时使用仓库实际箱型。
3. 未命中样本时走通用装箱算法：
   - 普通货物按尺寸和件数枚举箱型容量。
   - 同品多件容量按货物实物尺寸堆叠计算，`buffer_mm` 作为安全余量参考，不再重复加到每一件货物上。
   - 同一商品多件未命中整组样本时，推荐箱型不得小于该商品 `×1` 的已知或计算箱型；若因此被拉回单件下限，不再继续套用整数箱偏好，避免件数增加反而推荐更小箱或被二次改写。
   - 异形货物按堆叠等效尺寸估算，长边、宽边不变，高度随件数小幅叠加。
   - 混装按等效体积、最大单件约束和箱型容量综合判断。
4. 通用算法推荐半号箱且误差不大时，轻微偏向仓库常用整数箱型 `11`、`10`；当前相邻体积容忍系数为 `1.4`。
5. 气泡袋推荐仍使用原有算法，不受本轮箱型样本校准影响。

## 本轮仓库实际样本

| 组合 | 仓库实际箱型 |
| --- | --- |
| 140×1+702×2 | 8 |
| 702×2 | 7.5 |
| 155×1 | 11 |
| 155×2 | 10 |
| 155×3 | 8 |
| 139×1 | 11 |
| 139×2 | 11 |
| 140×3 | 7.5 |
| A113×6 | 11 |
| A160×1 | 10.5 |
| 184×1+140×1 | 7.5 |
| 139×1+140×1 | 11 |
| 164×2+165×1 | 6 |
| 163×1 | 11 |
| 164×1+184×1 | 6.5 |
| 112×1 | 6.5 |
| A113×2 | 11.5 |

## 调整原则

- 样本是当前最可信的验证依据，但允许后续发现录入错误后修正。
- 不从少量样本推导过强规则；先精确命中样本，再逐步观察未覆盖组合。
- 仓库偏好整数箱型，尤其是 `11`、`10`，但只在通用算法误差不大时偏向，不强制替换所有半号箱。
- 本轮不修改数据库结构，不修改尺寸管理原始数据，不调整气泡袋逻辑。

## 后续追加样本格式

新增样本时建议使用以下格式，便于直接加入校准表：

```text
货物编号×件数+货物编号×件数 -> 箱型
```

示例：

```text
702×1 -> 10
140×2+155×1 -> 8
```
